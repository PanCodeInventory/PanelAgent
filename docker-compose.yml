# =============================================================================
# PanelAgent - Docker Compose Stack
# =============================================================================
# Ports: frontend 3000, backend 8000
#
# Usage:
#   docker compose build     # build images
#   docker compose up -d     # start stack
#   docker compose ps        # check status
#   docker compose logs -f   # stream logs
#   docker compose down      # tear down
# =============================================================================

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: panelagent-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      BACKEND_CORS_ORIGINS: "http://localhost:3000,http://192.168.1.100:3000,http://192.168.1.105:3000"
    volumes:
      - ./data/quality_registry:/app/data/quality_registry
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: ../Dockerfile.frontend
    container_name: panelagent-frontend
    ports:
      - "3000:3000"
    environment:
      BACKEND_INTERNAL_URL: "http://backend:8000"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/').then(r=>{process.exit(r.ok?0:1)}).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
